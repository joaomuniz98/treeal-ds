import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as UploadStories from './Upload.stories';

<Meta of={UploadStories} />

# Upload

{/* Adicione aqui a descrição do componente Upload e seus tipos. */}

<Canvas of={UploadStories.Default} />

<Controls of={UploadStories.Default} />

---

{/* Adicione aqui as diretrizes de uso, formatos aceitos e estados. */}
