import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as CheckStories from './BaseCheckRadio.stories';

<Meta of={CheckStories} />

# Checkbox e Radios

{/* Adicione aqui a descrição dos componentes Checkbox e Radio e quando usar cada um. */}

<Canvas of={CheckStories.Default} />

<Controls of={CheckStories.Default} />

---

{/* Adicione aqui as diretrizes de uso, estados e acessibilidade. */}
